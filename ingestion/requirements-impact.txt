# Optional deps for the impact-analysis stage (councilhound.impact.*).
# LOCAL-RUN ONLY: never installed in the Fly jobs/API images — fairfaxva.gov
# IP-blocks datacenter ranges and these geo wheels would bloat the images.
# All imports of these packages are lazy (inside functions) so the base
# pipeline and cloud images never touch them.
# Intentionally omitted: pandana (pure-networkx fallback suffices for now),
# r5py/Java (connectivity module is a follow-up branch).
pydantic>=2.7
pyyaml>=6.0
numpy>=1.26
geopandas>=1.0
shapely>=2.0
pyproj>=3.6
pyarrow>=16.0
osmnx>=2.0
networkx>=3.2
duckdb>=1.0
gtfs-kit>=6.0
scikit-learn>=1.4
