"""Phase 4: embed transcript_chunks and document text for RAG retrieval."""


def embed_texts(texts: list[str]) -> list[list[float]]:
    """Return embeddings for a batch of texts. Pick an embedding model/provider
    when starting Phase 4 - not decided yet in the plan."""
    raise NotImplementedError("Phase 4 task")
