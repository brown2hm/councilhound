export default function Home() {
  return (
    <main>
      <h1>Fairfax Council KB</h1>
      <p>
        Placeholder page. Build the real timeline/dashboard here once the
        API's /meetings endpoint (Phase 4-5) returns real data.
      </p>
    </main>
  );
}
